This repo contains raw HTML+JS projects that fulfill a single function.

Libraries can only be used if they are pulled in via CDN.

All projects are to be published on GitHub Pages, so create accordingly.